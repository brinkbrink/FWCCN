import React, { Component } from 'react';
import { useRef } from "react";

export default class UpdateApplicant extends Component{
    render(){
        return(
            <div>
                <p>You are on the update applicant component</p>
            </div>
        )
    }
}