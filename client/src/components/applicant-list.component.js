import React, { Component } from 'react';
import { useRef } from "react";

export default class ApplicantList extends Component{
    render(){
        return(
            <div>
                <p>You are on the applicants component</p>
            </div>
        )
    }
}